# CAMSYS — Патч v1.6.52

⚠️ **17 файлов** (добавлен io_/spec_xml.py) — распакуйте с заменой поверх установки v1.6.0.

## Что исправлено
**Убраны петли в точках излома на путях углов (corner-2D/3D) во вьювере.**

На скриншоте были видны маленькие самопересечения («петли-острия») синего
пути угла на тугих изгибах. Причина найдена и устранена.

## Причина
- Синий путь угла (corner-rework) во вьювере строится как **эквидистанта
  фрезы**: сырой биарк-фрагмент контура смещается **по-сегментно**
  (`offset_polypath_uniform`).
- На стыках соседних offset-дуг сегмент k+1 начинается на 0.02–0.04 мм
  «позади» конца сегмента k → образуется микро-петля.
- `trim_self_intersections` эти петли **не видит**, потому что считает
  пересечения по **хордам** дуг, а петля возникает из-за самой **кривизны**.
  `join_polypath_corners(tol=0.01)` тоже не перекрывает зазор 0.02–0.04 мм.
- Основной нож (blade) этой проблемы не имеет: перед оффсетом он проходит
  сглаживание/merge, а углы это предобработку пропускают.

## Важно: реальный рез НЕ был затронут
Петли были **только в картинке** (визуальная эквидистанта вьювера).
В `.anc` угол пишется **сырым фрагментом + G42** (машинная компенсация) —
станок сам считает эквидистанту, явного смещённого пути в программе нет,
поэтому петель в реальном резе не было. Эмиссия `.anc` **не менялась**.

## Решение
Добавлена функция `despike_polypath()` (`geometry/path_offset.py`):
- находит петли через **истинное пересечение геометрии дуг** (arc-arc
  через пересечение окружностей, line-arc через квадратное уравнение) —
  НЕ по хордам и НЕ через сэмплинг, поэтому ловит даже крошечные петли на
  остриях, которые полилинейный подход пропускает;
- обрезает пересекающиеся сегменты по точке пересечения и выбрасывает
  петлю между ними, **сохраняя первую и последнюю точки** (к ним крепятся
  lead-in/out);
- ищет только локальные пары сегментов (петли на эквидистанте локальны по
  индексу) — проход линейный, быстрый даже на крупных углах;
- чистая реализация на Python (без shapely).

Вызывается во вьювере (`ui/viewer_2d.py`) в ветке углов сразу после
`join_polypath_corners`. Трогаются **только углы**; основной путь ножа,
подбор лидов и «пологость» перехода угол→заход не изменены.

## Проверка
- Тесты: **143/143**.
- На реальных `.ai` (123308, 41000, 41316) при пороге угла 0.575 (реальный
  дефолт): истинных самопересячений на контурах углов **221 → 0**, концы
  путей сохранены, ~0.6 мс на угол. Острие ножа со скриншота (123308,
  нож 1): петля убрана (см. картинку до/после).

## Изменённые файлы (по существу)
- `camsys/geometry/path_offset.py` — новая `despike_polypath()` + хелперы
  истинного пересечения дуг.
- `camsys/ui/viewer_2d.py` — вызов despike в ветке углов; фикс стороны
  лида и фикс зума (см. ниже).
- `camsys/__init__.py` — версия 1.6.1.

## Доп. исправления в этой сборке
**Сторона лида на углах.** despike меняет число сегментов эквидистанты,
а сторона «тела» угла бралась как `segments[len//2]` — из-за этого лид на
некоторых углах разворачивался не в ту сторону. Теперь эталон стороны
фиксируется ДО despike (старт и касательную despike и так не трогает), лид
строится как в v1.6.0.

**Зум колесом — к курсору.** Якорь трансформации стоял AnchorUnderMouse и
одновременно работала ручная компенсация сдвига — на flip-сцене (Y вверх)
эти два механизма конфликтовали, картинка прыгала к началу координат.
Теперь в `wheelEvent` явно ставится NoAnchor, зум центрируется под
курсором.

Остальные файлы из набора — без изменений (для целостности установки).

---

# CAMSYS — Патч v1.6.52 (доп. к 1.6.2)

## Сторона лида на вогнутых (внешних) углах
На облачных контурах (напр. 123308 нож2) лид на вогнутом углу уходил
НАРУЖУ — от тела угла, пересекая контур. Причина: сторона выбиралась как
`_lead_same_as_body = _convex`, т.е. для вогнутого — противоположно телу
(это было допущение, не подтверждённое). Диагностика показала, что наружная
сторона там ещё и коллизирует с контуром.

Исправлено: лид всегда строится В СТОРОНУ ТЕЛА угла (`_lead_same_as_body =
True`). Выпуклые углы (прямоугольник) и так использовали эту ветку —
поэтому меняется ТОЛЬКО вогнутый случай, подтверждённое поведение
выпуклых не затронуто.

Файл: `camsys/ui/viewer_2d.py`. Тесты 143/143.

---

# CAMSYS — Патч v1.6.52 (откат ошибочного v1.6.3)

## Откат неверного изменения стороны лида
В v1.6.3 я ошибочно сделал лид угла ВСЕГДА к телу (`_lead_same_as_body =
True`). Это увело лиды на вогнутых углах К ДЕТАЛИ — неверно (можно
зарезать деталь). Откат к `_lead_same_as_body = _convex`.

Проверка (123308): на прямоугольнике (geom4, подтверждён как верный) лиды
идут К ЦЕНТРУ (в отход/полость = от детали); на облачном ноже (geom2)
вогнутые лиды идут ОТ ЦЕНТРА (наружу = от детали). Правило: лид
противоположен знаку эквидистанты угла — как и на эталонном прямоугольнике.

Файл: `camsys/ui/viewer_2d.py`. Тесты 143/143.

---

# CAMSYS — Патч v1.6.52

## Сторона лида угла = ЗНАК ЭКВИДИСТАНТЫ (а не выпуклость)
Раньше сторона лида угла бралась из per-corner ВЫПУКЛОСТИ (`_convex` через
центр дуги относительно тела). На облачных контурах это давало
рассогласование: у части углов лид уходил не в ту сторону, что эквидистанта
(«проскакивал» один-два угла — лид к детали).

Теперь сторона лида берётся напрямую из ЗНАКА СМЕЩЕНИЯ ЭКВИДИСТАНТЫ:
вектор от старта контура-фрагмента к старту эквидистанты — куда эквидистанта
отодвинута от программного контура. Лид строится в ТУ ЖЕ сторону (дальше от
детали). Это правило «рисуем лиды тем же знаком, что эквидистанту».

Проверка (123308): на облачных ножах лиды теперь согласованы с синей
эквидистантой у всех углов; на прямоугольниках (geom4/5) поведение прежнее
(лид к центру — подтверждено ранее). Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Исправлен ЗНАК эквидистанты у отдельных углов (проскочивший угол)
На облачном ноже (123308 geom1) у пары углов (c1, c3) эквидистанта уходила
ВНУТРЬ (к детали), а у всех соседних вогнутых углов — наружу. Корень: при
определении выпуклости бралась «первая дуга <2мм» во фрагменте, и у этих
углов это была КРУПНАЯ дуга фланга (R~1.5–1.9), а не тесная дуга скругления
угла (R~0.68). Центр крупной дуги попадал внутрь тела → ложный «выпуклый» →
эквидистанта и лид разворачивались не в ту сторону.

Исправление: для определения выпуклости берём САМУЮ ТЕСНУЮ дугу (мин.
радиус) — это и есть реальное скругление угла.

Проверка (123308): у всех 12 углов geom1 эквидистанта и лиды теперь СНАРУЖИ
(от детали) — согласованы между собой; geom2 — так же; прямоугольники
(geom4/5) без изменений (к центру). Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Новое: «Отключить путь (выделенный)» в правой кнопке
В контекстном меню toolpath'а добавлен пункт **«Отключить путь
(выделенный)»** (и обратный «Включить путь»). Отключает ТОЛЬКО выбранную
операцию (этот путь/угол), без синхронизации со всем ножом — в отличие от
«Отключить нож». Практично, чтобы точечно убрать отдельный проскочивший
угол из превью и экспорта.

Файл: `camsys/ui/main_window.py`.

## Замечание по превью углов (важно, на будущее)
Подтверждено: превью строит геометрию угла НЕЗАВИСИМО от .anc, а .anc
пишет угол сырым фрагментом + G42 (эквидистанту считает станок). Поэтому
превью и .anc могут расходиться (напр. «угол 10» смотрит не туда в превью,
хотя в G-code всё верно). Правильное решение — строить превью углов и их
лидов ИЗ .anc (инфраструктура частично есть: parse_anc_movements,
parse_anc_leads, match_tangents_to_ops). Это отдельная задача.

---

# CAMSYS — Патч v1.6.52

## Диагностика угла в правом клике
На угле, который в превью смотрит не туда: правый клик → **«Диагностика
угла…»**. Покажет, что вьювер насчитал для ЭТОГО угла:
apex, corner_radius, rounding_R (радиус дуги, взятой для выпуклости),
rounding_center, convex, inward (знак эквидистанты), forced_lead_side
(сторона лида).

Зачем: в моём точном воспроизведении 123308 (реальные corner-ops
экспортёра, реальные параметры, cutting_params, anc_tangents) ВСЕ углы
облачных ножей рисуются верно — сбой не повторяется. Значит в конкретном
сеансе на этот угол приходит другое состояние/вход. Пришли мне цифры из
диагностики того угла — и я точно увижу, где расходится.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Сторона лида угла — устойчивое правило (нашлось по диагностике)
Диагностика показала суть: на ОДНОМ угле (apex 306,153, convex=False,
inward=True) приложение давало forced_lead_side=right, а точное
воспроизведение — left. Причина: прежняя логика v1.6.5 брала сторону по
ВЕКТОРУ (offset_start − contour_start), а у пограничных углов он почти
нулевой → знак скалярного произведения переворачивался от численных
мелочей. Отсюда «проскочил один».

Теперь сторона лида определяется УСТОЙЧИВО: лид ставится на ту же сторону
контура (внутри/снаружи замкнутого ножа), где лежит ЭКВИДИСТАНТА — через
_point_in_polypath хорошо разнесённых точек (середина эквидистанты и
кончик пробного лида), без хрупкого вектора. Правило то же — «лид тем же
знаком, что эквидистанта, от детали», но детерминированно.

Проверка (123308, реальные corner-ops экспортёра, tip 0.8 и 1.2): у ВСЕХ
углов лид на той же стороне, что эквидистанта — 0 расхождений; угол
(306,153) → сторона left (снаружи = от детали); прямоугольники — к центру
(как эквидистанта, подтверждено). Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`. (Диагностика угла из v1.6.8 сохранена.)

---

# CAMSYS — Патч v1.6.52

## Угол/высота: убраны тихие умолчания (XML)
По просьбе: если угол заточки / высота ножа НЕ подтянулись из specification
XML — поле теперь чистится в «не задано» (угол → «—», высота → 0) и
подсвечивается розовым, а НЕ остаётся дефолтом 70/0.440. Плюс защита:
при экспорте с незаданным углом/высотой — явная ошибка (не даст уехать в
работу с неверным значением).

## XML выбирался «первый попавшийся»
`find_spec_xml` брал первый specification_*.xml в папке XML без сверки
номера. Если там лежал файл другого заказа — подтягивался чужой (или
ничего). Теперь при нескольких файлах предпочитается тот, чей номер
совпадает с номером папки заказа. (Парсер сам читал 124222 верно:
angle=80, height=0.448 — проблема была в выборе файла/применении.)

Файлы: `camsys/ui/main_window.py`, `camsys/io_/spec_xml.py`.

---

# CAMSYS — Патч v1.6.52

## Угол/высота полностью убраны из умолчаний
Раньше 70° и 0.440 подставлялись при старте, сбросе и «применить дефолты».
Теперь угол и высота НЕ имеют тихого дефолта нигде:
- старт приложения: угол «—», высота 0, оба розовые;
- сброс параметров: то же;
- «Применить значения по умолчанию»: угол/высоту не трогает (ставит «не
  задано» + розовое), берёт их только из XML спецификации.
- get_params_dict при незаданном возвращает 0 (не падает на живом UI).
- Экспорт с незаданным углом/высотой — отказ с понятным сообщением
  (розовые поля), чтобы не уехать в работу с умолчанием.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Смещение лида: равномерно от правого верхнего угла (RT)
Раньше знак смещения инвертировался по направлению обхода полипаса
(`user_offset if _polypath_is_cw else -user_offset`) — из-за этого один и
тот же offset двигал ножи с разным обходом в РАЗНЫЕ стороны (вход/выход
«раскидывало», немонотонно: −1 мог оказаться левее −4).

Теперь смещение трактуется геометрически и РАВНОМЕРНО: 0 = правый верхний
угол (RT), <0 = влево по верху (уменьшение X), >0 = вправо — одинаково для
всех ножей и обоих проходов (внутр./внешн. двигаются вместе).

Проверка (123308, geom4/1/2): на всех ножах X старта лида убывает
монотонно 0→−1→−4→−6, внутренний и внешний в одну сторону. Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.
(Авто-предложение лида на острых углах — отдельно, следующим шагом.)

---

# CAMSYS — Патч v1.6.52

## Равномерное смещение — ТЕПЕРЬ И В ЭМИТТЕРЕ .anc (главное)
В v1.6.12 равномерное смещение было только во вьювере, но основные лиды
вьювер берёт из .anc (anc_tangent), а смещение реально применяет ЭМИТТЕР
(mtx_anderson.py). Там был тот же знак-флип по намотке. Перенёс туда тот же
геометрический подход: 0 = RT-угол, <0 = влево по верху (меньше X), >0 =
вправо — одинаково для всех ножей и обоих проходов.

Проверено: блок смещения в эмиттере исполняется, оба прохода уезжают в одну
сторону (OUTSIDE eff=−4, INSIDE eff=+4 → визуально оба влево). Тесты 143/143.

ВАЖНО про поведение:
- Смещение задаёт ТОЧКУ СТАРТА лида вдоль верха (0 = RT-угол). Оно НЕ
  «заталкивает» лид в угол — лид всё равно отходит от точки старта.
- «Заезд лида в свободный угол» и «уменьшить угол/укоротить, если не
  влезает» — это АВТО-ПОДБОР (режим «Авто-подбор»), отдельная логика,
  следующим шагом. В режиме «Все элементы» авто-подбор ВЫКЛЮЧЕН — берётся
  смещение точно как задано.

Файлы: `camsys/post/mtx_anderson.py`, `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## 0 = правый верхний угол ТЕПЕРЬ и на облачных ножах (главное)
Нашлось, почему «0 не идёт в угол»: привязка нуля (`shift_start_to_corner
("RT")` + top_line) находила RT только на ПРЯМОУГОЛЬНИКАХ; на облачных/
криволинейных ножах промахивалась на 5–10мм (geom1 Δ=5.79, geom2 Δ=9.80),
и смещение шло «от текущей позиции», а не абсолютно от угла.

Теперь 0 = ИСТИННАЯ верхне-правая вершина ножа (max X+Y) — однозначно для
любой формы. Проверено на 123308 (прямоугольник + два облака): offset=0
попадает в RT ровно (Δ=0.00), а −1/−4/−6 дают монотонный сдвиг влево от
угла, одинаково для всех ножей и обоих проходов. Абсолютно от угла, не «от
текущей».

Применено И в эмиттере .anc (mtx_anderson.py), И во вьювере (fallback без
anc_tangent). Тесты 143/143.

Напоминание: «автоподбор» (заезд в угол, уменьшение угла/укорачивание) —
только в режиме «Авто-подбор». В «Все элементы» он ВЫКЛЮЧЕН — берётся
смещение точно. Это делаю следующим шагом.

Файлы: `camsys/post/mtx_anderson.py`, `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## 0 = верхне-правый угол BBOX; внутр/внешн выровнены
По замечанию: привязка к вершине контура давала разную точку → внутр/внешн
не совпадали. Теперь 0 = верхне-правый угол BBOX элемента (maxX, maxY) —
ЕДИНАЯ фиксированная точка для обоих проходов и любой формы.

Сдвиг смещения сделан НАДЁЖНЫМ по X: offset<0 = старт уезжает ВЛЕВО
(меньше X), >0 = вправо — берётся то направление обхода, что реально
двигает X в нужную сторону (у угла tangent ненадёжен). 

Проверено (123308, все ножи): точки крепления внутр/внешн совпадают точно
(|ΔX|=0.00) на любом offset; 0 = ровно угол bbox; −1/−2/−4/−6 монотонно
влево, абсолютно от угла. (Кончики лидов внутр/внешн расходятся — это сами
лиды отходят в разные стороны, так и должно быть.)

Применено и в .anc-эмиттере, и во вьювере. Тесты 143/143.

Файлы: `camsys/post/mtx_anderson.py`, `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Диагностика смещения (правый клик по ножу)
Правый клик по ножу → «Диагностика смещения…». Покажет ПО КАЖДОМУ проходу
(OUTSIDE=Внутренний, INSIDE=Внешний):
- offset — какое смещение реально пришло на проход;
- из .anc — взялась ли точка из тангенса .anc (и его координаты);
- старт — куда встал старт лида.

Прогони на прямоугольнике −5 / 0 / 5 / 2 и сравни: если у внутреннего
«старт» не меняется при смене offset — сразу видно, где рвётся.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Диагностика смещения — в лог-файл
Теперь при каждом пересчёте путей диагностика смещения по каждому проходу
пишется в файл **~/camsys_offset_diag.log** (домашняя папка пользователя,
Windows: C:\Users\<имя>\camsys_offset_diag.log). Строка на проход:
время | нож | сторона(Внутр/Внешн) | offset | from_anc + tangent | start.

Прогони −5 / 0 / 5 / 2 (пересчитывая каждый раз) и пришли этот лог —
видно будет, меняется ли offset и start у внутреннего прохода.
(Пункт «Диагностика смещения…» в правом клике тоже остался.)

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Смещение — вдоль ВЕРХНЕЙ грани от угла (исправлено окончательно)
По логу вскрылось: старое смещение двигало старт ПО ПЕРИМЕТРУ и заворачивало
за угол вниз по стороне — отсюда каша (offset 0 и 5 давали одну точку,
+offset «улетал», внутр≠внешн).

Новая функция `shift_start_to_top_x`: старт двигается строго вдоль ВЕРХНЕЙ
грани от верхне-правого угла КОНТУРА:
- 0 = угол;
- <0 = влево по верху, монотонно (−5 дальше −2);
- >0 = зажим у угла (правее верхней грани нет);
- ЕДИНАЯ точка для обоих проходов → внутр/внешн выровнены (ΔX=0).

Проверено в .anc (эмиттер) на прямоугольнике: off −5→X211.16, −2→214.16,
0→216.16(угол), +2→216.16(зажат), внутр=внешн на каждом. Облака — так же
чисто. Тесты 143/143.

Применено в эмиттере (.anc) и вьювере. Диагностика смещения (лог) на месте.

Файлы: `camsys/geometry/path_offset.py`, `camsys/post/mtx_anderson.py`,
`camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Плюс-смещение — вниз по правой грани (как было исторически)
В v1.6.18 «+» зажимался у угла. Восстановлено историческое поведение:
- 0 = верхне-правый угол;
- < 0 = влево по ВЕРХНЕЙ грани (−5 дальше −2);
- > 0 = вниз по ПРАВОЙ грани (+2, +5 — по правой стороне вниз);
- внутр/внешн выровнены (Δ=0) на всех offset.

Проверено (123308, все ножи): geom4 +2→Y144.61, +5→Y141.61 (вниз по
правой, X=const); облака аналогично. Тесты 143/143.

Файл: `camsys/geometry/path_offset.py`.

---

# CAMSYS — Патч v1.6.52

## Смещение по alphacam-модели: 0 = диагональная точка угла
По подсказке (как в макросах alphacam): 0 = точка, где ДИАГОНАЛЬ из
верхне-правого угла bbox к центру пересекает контур. На скруглённом углу
это его 45°-точка — ОДНОЗНАЧНА для обоих проходов, поэтому внутр/внешн
садятся в одну точку и совпадают (раньше в самой вершине два прохода
выбирали разные грани → «верх стоит, низ уехал вправо»).

От 0-точки: offset<0 = влево по ВЕРХНЕЙ грани, offset>0 = вниз по ПРАВОЙ.

Проверено в .anc на реальном 124222: off −5→(61.39,63), 0→(66.29,62.54),
+2→(67,60.75), +5→(67,57.75); внутр=внешн на КАЖДОМ offset. Тесты 143/143.

Новая функция `shift_start_from_diagonal_zero` в geometry/path_offset.py,
подключена в эмиттере (.anc) и вьювере.

Файлы: `camsys/geometry/path_offset.py`, `camsys/post/mtx_anderson.py`,
`camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Превью лидов ножа: убрал кривой парсинг тангенса из .anc
Причина «верх стоит, низ уехал вправо» при 0/+2: приложение передавало во
вьювер точки из compute_anc_tangents, и на сшитой раскладке у угла парсинг
давал OUTSIDE неверную точку (уезжала на другую грань на ~5мм) → внутр/
внешн расходились.

Теперь вьювер НЕ использует anc-тангенс для лидов ножа: эмиттер и вьювер
считают старт ОДНОЙ функцией (shift_start_from_diagonal_zero), поэтому
собственный расчёт вьювера = .anc, но без ошибок парсинга.

Проверено (124222, с эмуляцией anc_tangents — игнорируются): на всех offset
внутр/внешн расходятся ровно на ширину полосы реза (~1.15мм, это две
кромки — норма), на одной грани, монотонно (− верх, + правая). Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Авто-подбор снова работает (не глохнет от anc_tangents)
Причина «Авто-подбор ничего не делает»: auto_avoid принудительно
выключался, если у операции есть anc_tangents (`... and not
_use_anc_position`), а приложение их ВСЕГДА передаёт. Итог — в Авто-режиме
лид не двигался вовсе.

Поскольку с v1.6.21 anc_tangent для позиции больше не используется (старт
считаем сами, как эмиттер), подавление auto_avoid убрано. Теперь в режиме
«Авто-подбор» auto_avoid реально ищет позицию (сдвиг + подбор угла/длины,
как и было в алгоритме). В «Все элементы» — по-прежнему точное смещение
без авто.

Проверено (124222, с эмуляцией anc_tangents): Авто-подбор двигает лиды,
Все элементы — держит у угла. Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Лог авто-подбора (диагностика среднего столбца)
В лог ~/camsys_offset_diag.log теперь пишется результат авто-подбора по
каждому проходу: AUTO (вкл/выкл), collision (нашлась ли чистая позиция),
крепление (куда сел лид). Прогони в режиме «Авто-подбор» и пришли лог —
будет видно, что происходит со средними ножами (двигается/красный/стоит).

В моём воспроизведении на 124222 v1.6.22 авто-подбор находит позиции для
ВСЕХ столбцов (средние тоже, 0 коллизий) — «средний ничего» пока не
повторяется, лог поможет увидеть разницу (вероятно shapely-детекция).

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Авто-подбор: убран завышенный запас безопасности во вьювере
Корень «средний столбец дёргается/ничего»: вьювер вызывал plan_lead_in с
запасом safety_factor=3.0 (≈1.73мм), а зазоры в тесной раскладке ~2мм.
Лид в 1.49мм от соседа (реально НЕ пересекает) считался коллизией → auto
делал микро-нудж (0.56мм, не видно) в середине и большие перелёты по краям.
Эмиттер при этом использовал 1.2 → превью не совпадало с .anc.

Теперь вьювер тоже 1.2 (как эмиттер). Результат на 124222: во ВСЕХ столбцах
(и средних, и крайних) лид держится у угла (collision=False), без нуджей и
перелётов; превью совпадает с .anc. Реальные пересечения по-прежнему ловятся
и подбираются (сдвиг → угол/длина, как в алгоритме). Тесты 143/143.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/geometry/lead_collision.py`.

---

# CAMSYS — Патч v1.6.52

## Датчик обрезки контура (диагностика круг→полукруг)
Подтверждено из твоего .anc: мелкий круг эмитится наполовину (нижняя часть
Y144–149 выброшена, дуги вразнобой) — портит деталь. Воспроизвести в
песочнице не удаётся (изолированный нож не эмитится). Поэтому в эмиттер
добавлен датчик: по каждому проходу в файл ~/camsys_emit_diag.log пишется
«контур_сегм (сколько в контуре) / эмит_сегм (сколько пошло в .anc) /
start_t/end_t / размах w×h».

Прогони экспорт своего заказа (124173) и пришли **~/camsys_emit_diag.log**.
По строке проблемного круга увижу: контур_сегм=32, а эмит_сегм=? — если
меньше, обрезка выше эмиссии (start_t/end_t или offset); если 32, а в .anc
9 — проблема в самом выводе. Это укажет точное место.

Файл: `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## КРИТИЧНО: круг→эллипс/полукруг при экспорте — ИСПРАВЛЕНО
Датчик из v1.6.25 показал: Knife_56 (124173) в контуре имеет 2 дуги, и одна
из них — разворот **214°** (>180°). Эмиттер выводил её как R-дугу
(G3 X Y R), а R-дуга однозначна ТОЛЬКО при <180° — станок по знаку R берёт
МЕНЬШУЮ дугу. В итоге вместо полукруга получался тонкий серпик → круг
резался наполовину (нижняя часть отсутствовала) → брак детали.

Исправление: перед эмиссией дуги с разворотом >180° дробятся на равные
под-дуги ≤90° (однозначные по R). Проверено на Knife_56: 214°→3×71.5°,
bbox круга стал полным 9.77×9.79 (как в Alphacam-эталоне). Alphacam так и
делает — дробит круг на мелкие дуги. Тесты 143/143.

Затрагивает только ножи с дугами >180° (в этом заказе — 1, Knife_56);
остальные без изменений. Датчик обрезки (emit_diag.log) убран.

Файл: `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## Дробление дуг: порог >90° (квадрантный лимит)
По просьбе: дробим R-дуги с разворотом >90° (а не только >180°). Так
однозначно даже у дуг близко к 180°, соответствует $525 «Quadrant Limit
arcs» и практике Alphacam (мелкие дуги). Круг Knife_56 и любые крупные
дуги выводятся квадрантами ≤90° — R всегда корректен.

Файл: `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## Круг всё ещё эллипс — НАСТОЯЩАЯ причина найдена
Дробление дуг >90° из v1.6.27 стояло СЛИШКОМ РАНО: после него `segments`
ПЕРЕЗАПИСЫВАЛСЯ строкой `segments = polypath.segments` (после автоподбора
лида) — и большая дуга 214° возвращалась. Поэтому эллипс оставался.

Перенёс дробление на дуги >90° прямо ПЕРЕД циклом эмиссии (на финальный
segments, после всех этапов лида/overlap). Проверено датчиком: Knife_56
теперь 2 дуги → 5 под-дуг (все ≤90°), большая 214° реально дробится.

Файл: `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## Заходы в .anc = заходы в превью (вариант A)
Причина «превью≠gcode» (40 ножей, расхождение 2–10мм): вьювер планирует
авто-подбор на offset-пути (центр фрезы, упрощённый+объединённый контур),
а эмиттер — на СЫРОМ контуре. Разная геометрия → авто-подбор находит разные
свободные позиции → заходы разъезжаются.

Сделан единый источник: вьювер СОХРАНЯЕТ вычисленную точку захода на op
(op.attributes['_lead_entry'][сторона]); эмиттер её ЗАБИРАЕТ, ставит старт
сырого контура в ближайшую к ней точку и свой авто-подбор выключает. Итог:
.anc даёт заходы ровно как в превью.

Проверено: эмиттер использует сохранённую точку (старт → ближайшая точка
контура), оба прохода сходятся к общей точке. Тесты 143/143. Диагностический
лог авто-подбора убран.

ВАЖНО: точка берётся из последнего пересчёта путей в превью. Если поменял
параметры — сначала «Пересчитать пути», потом экспорт (иначе заход из старого
расчёта). Без превью-расчёта эмиттер работает как раньше (свой авто-подбор).

Файлы: `camsys/ui/viewer_2d.py`, `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## ОТКАТ v1.6.29 — лишние круги в .anc
v1.6.29 (заход из вьювера через shift_start_to_point в эмиттере) вызвал
ПОЯВЛЕНИЕ лишних окружностей в .anc (перепривязка старта сырого контура к
offset-точке ломала контур на части ножей). Это опаснее исходного
рассогласования — откатываю немедленно.

Убрано: сохранение _lead_entry во вьювере и его использование в эмиттере.
Эмиттер снова планирует авто-подбор как в v1.6.28.

СОХРАНЕНО: фикс круга (дробление дуг >90°, v1.6.28) — Knife_56 остаётся
полным кругом, лишней геометрии от него нет (проверено: 0 невозможных дуг
chord>2R, 0 дуг R>50 в эмиссии).

Задача «заходы .anc = превью» (вариант A) остаётся открытой — переделаю
безопасным способом (без перепривязки сырого контура).

Файлы: `camsys/ui/viewer_2d.py`, `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## Заходы превью = .anc (вариант A, безопасно, viewer-only)
Причина расхождения: вьювер планировал авто-подбор на OFFSET-пути, эмиттер —
на СЫРОМ контуре → разные свободные точки (2–10мм).

Теперь ВЬЮВЕР считает авто-подбор на СЫРОМ контуре той же функцией и с тем
же кэшем/safety, что эмиттер (geom.polypath + diagonal-zero) → точка захода
совпадает с .anc ПО ПОСТРОЕНИЮ. Найденный сдвиг переносится на offset-
отображение по ДОЛЕ длины кольца через shift_start_along_contour (без
re-root контура — поэтому без артефактов, в отличие от отката v1.6.29).

ВАЖНО: правка ТОЛЬКО во вьювере. Эмиттер и .anc НЕ изменены → нулевой риск
паразитов/порчи материала. Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Откат viewer-эксперимента (заход) к стабильному
Попытка A (вьювер планирует на сыром + привязка offset) давала лишь
частичное совпадение (10/30) и рисковала глючным превью, а проверить
превью визуально в песочнице я не могу. Откатил вьювер к стабильному
поведению v1.6.30 (план на offset). Превью снова предсказуемое.

СОХРАНЕНО: фикс круга (дробление >90°), откат опасного v1.6.29 (без
паразитов). .anc корректен и безопасен — эмиттер не трогался вообще.

Задача «заход превью = .anc» остаётся: она косметическая (материал режется
верно по .anc), и её надёжное решение требует правки визуализации вьювера
с визуальной проверкой, которой у меня в песочнице нет.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Заходы .anc = превью — возврат, теперь без паразитов
Ты прав: замысел v1.6.29 был верным (заход как в превью — логичнее, меньше
холостых перемещений станка), а паразитные круги шли от того, что re-root
контура делался БЕЗ проверки — битый контур давал вырожденный лид.

Возвращено с защитой:
- вьювер сохраняет вычисленную точку захода на op;
- эмиттер ставит туда старт контура, НО сначала проверяет валидность
  re-root: контур остался замкнутым, число сегментов ±2, длина ±1%.
  Не прошло — откат на обычный авто-подбор (никаких битых контуров);
- касательную в новой точке строит сам plan_lead_in по полипасу, отдельно
  задавать не нужно (это и была дыра v1.6.29).

Проверка на 124173 со 120 заданными точками захода: в .anc **0** невозможных
дуг (chord>2R) и **0** дуг R>50 → паразитных окружностей нет. Тесты 143/143.

ПОРЯДОК: сначала «Пересчитать пути» (вьювер запишет заходы), потом Экспорт.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## Паразитный круг — найден по твоему фрагменту N781-786
Твои строки показали суть: 5 дуг с ОДИНАКОВЫМ R29.97927, замыкающихся в ту
же точку = идеальная окружность, разбитая на ~72°. Это след МОЕГО дробления
дуг (v1.6.27+): у ВЫРОЖДЕННОЙ дуги (начало≈конец после offset/despike)
разворот вычислялся как 360°, и дробление «раздувало» её в полный круг из
4-5 дуг. Отсюда «подходит к выходу и делает полный круг».

Исправление: вырожденные дуги (хорда < 1e-4) НЕ дробятся — выводятся как
есть. Настоящие крупные дуги (круг Knife_56, 214°) дробятся как прежде.

Проверка на 124173: паразитных окружностей (>=4 дуг одного R, замкнутых)
в .anc — **0**. Тесты 143/143.

Файл: `camsys/post/mtx_anderson.py`.

---

# CAMSYS — Патч v1.6.52

## Сохранение сборки: настройки + макет, в папку CAD первого ножа
Меню Файл → «Сохранить настройки...» теперь сохраняет ПОЛНУЮ сборку для
повтора заказа:
- параметры реза (как раньше) + пост;
- МАКЕТ: отключённые ножи/пути (excluded, disabled) и ручные заходы
  (lead_override) — то есть ручные решения оператора.

Диалог по умолчанию предлагает **папку CAD** (ищется рядом с исходным .ai:
сама папка CAD или ближайшая вверх по дереву) и имя файла = имя .ai + .json.
Так сборка лежит вместе с макетом и легко находится при повторе.

Ключ состояния сделан стабильным (имя+вид операции): при повторной загрузке
.ai id операций генерируются заново, поэтому по id не восстанавливалось.
Проверено: сохранение → загрузка в новую сессию → excluded/lead_override/
disabled восстановлены 1/1/1 + параметры. Тесты 143/143.

Загрузка — Файл → «Загрузить настройки...» (поднимает .ai, параметры и макет).

Файлы: `camsys/core/session.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## 1. Загрузка — из папки CAD (как сохранение)
«Загрузить настройки...» теперь открывается в папке CAD рядом с текущим
.ai (как и сохранение), а не в последнем случайном каталоге.

## 2. После загрузки строятся пути
Раньше поднималась «голая» геометрия. Теперь после загрузки сборки пути
пересчитываются сразу — заказ открывается в рабочем виде.
(Пути в файле не хранятся: сохраняются параметры + решения оператора,
пути строятся из них.)

## 3. Галка «Острые углы 2D» больше не стоит по умолчанию
Найдена причина: при сборке параметров стояло
`params.get('generate_corner', True)` — если ключа нет, подставлялось True,
и углы включались даже там, где их нет (проверено: детектор на скруглении
R2 углов не находит — то есть виноват был дефолт, не детекция).
Теперь дефолт False: галка включается ТОЛЬКО когда детектор нашёл углы.

Тесты 143/143.

ОСТАЁТСЯ (по твоему п.2): список заказов и активные/неактивные номера пока
не сохраняются — это отдельная структура UI, сделаю следующим шагом.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## 1. Загрузка — из БАЗОВОГО пути настроек
Диалог загрузки открывается в базовом пути экспорта из настроек (хранилище
заказов), а не от текущего .ai: при загрузке ещё неизвестно, какой заказ
грузим. Сохранение по-прежнему предлагает папку CAD текущего заказа.

## 2. Список заказов сохраняется и восстанавливается
В сборку пишутся настройки заказов (_order_settings) — при загрузке
дропбокс заказов поднимается как был. Проверено: два заказа
(124173/124372) сохранены → восстановлены; решения оператора тоже.

## 3. Галка «Острые углы 2D» на круглых ножах — исправлено
Причина: детекция шла по ВСЕМ ножам макета, и если углы были хоть у одного
ножа, галка вставала для всех заказов — в т.ч. там, где ножи круглые.
Теперь детектируем ТОЛЬКО по ножам текущего заказа. (Сам детектор верен:
на 124173 из 60 ножей углы у 11 фигурных, у круглого — нет.)
Плюс убран дефолт generate_corner=True (v1.6.36).

Тесты 143/143.

Файлы: `camsys/core/session.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Пути после загрузки сборки — теперь строятся
Авто-пересчёт из v1.6.36 вызывался синхронно сразу после _refresh_all(),
когда сцена и АКТИВНЫЙ ЗАКАЗ ещё не готовы (они ставятся асинхронно) —
поэтому пути не появлялись. Теперь вызов отложенный (300мс), и если он всё
же не проходит, причина показывается в статус-баре, а не глохнет молча.

Про галку «Острые углы 2D» на твоём скрине: активен 124173 — там ножи
фигурные, углы РЕАЛЬНО есть (детектор находит углы у 11 из 60 ножей), так
что галка стоит верно. На заказе с круглыми/прямоугольными ножами она
теперь сниматься будет (v1.6.37: детекция по ножам текущего заказа).

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Галка «Острые углы» на прямоугольниках — две настоящие причины
1) Фильтрация ножей по заказу (v1.6.37) НЕ РАБОТАЛА: я обращался к
   несуществующим `_stitch_map` / `get_region_for_knife`, код молча падал в
   except и брал ВСЕ ножи макета. Теперь через штатные
   `session._stitch_info.get_region_by_order(order)` → `region.knife_ids`.
2) Детекция вызывалась ТОЛЬКО при загрузке файла. При смене заказа в
   дропбоксе она не перезапускалась — галка оставалась от предыдущего
   заказа. Теперь `_auto_detect_corner_programs()` вызывается и при смене
   заказа.

Итог: галка отражает ножи ИМЕННО активного заказа — на прямоугольниках/
кругах снимается, на фигурных встаёт.

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Загрузка сборки: заказы в дропбоксе + пути
Нашлась корневая причина обоих симптомов: `load_state_from_json` грузит .ai
НАПРЯМУЮ, минуя цепочку открытия файла. Поэтому не выполнялись разбор
сшивки (список заказов), детекция углов, создание операций и авто-репер —
дропбокс «№» оставался пустым и неактивным, а пути строить было не из чего
(нет активного заказа → нечего пересчитывать).

Теперь после загрузки .ai из сборки выполняется та же цепочка, что при
обычном открытии: _last_ai_path → операции + sort_by_grid → авто-репер X →
_analyze_and_setup_stitch (заказы в дропбокс) → детекция углов. Затем
_refresh_all и отложенное построение путей (v1.6.38).

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Пути после загрузки — для ВСЕХ заказов, где они были
Раньше при загрузке пересчитывался только активный заказ: переключаешься на
другой — путей нет.

Теперь в сборку пишется список заказов, у которых пути были построены
(`orders_with_paths`), и при загрузке пути строятся для КАЖДОГО из них
(с переключением заказа и возвратом на исходный).

Почему не храним сами траектории: они зависят от версии алгоритма и
параметров — сохранённые «сырые» пути после обновления программы могли бы
разойтись с реальным .anc, что опаснее. Пересчёт из сохранённых параметров
и решений даёт тот же результат и всегда согласован с эмиттером.

Проверено: список заказов с путями сохраняется и восстанавливается.
Тесты 143/143.

Файлы: `camsys/core/session.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Второй заказ оставался без путей — исправлено
Причина: я переключал заказ через `findText(номер)`, а в комбобоксе текст
вида «124173 (копия 1) (60 ножей)» — ключ заказа лежит в userData. Поиск
не находил, второй заказ молча пропускался, и пересчитывался только
первый (дважды).

Теперь заказ ищется по userData (тот же ключ, что в _current_stitch_order и
в кэше путей), между переключением и построением добавлен processEvents,
чтобы смена заказа успевала примениться до пересчёта.

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Индивидуальные настройки заказов теперь реально сохраняются
Ты прав: пересчёт без параметров бесполезен. Нашлись ТРИ причины, почему
per-order настройки не доезжали до файла и обратно:

1) Настройки АКТИВНОГО заказа снимаются в словарь только при переключении
   на другой заказ. При сохранении сборки его правки (направление,
   смещения, исключения) в файл не попадали. Теперь перед записью
   выполняется _save_order_settings(текущий).

2) op_attrs привязаны к op.id, а при загрузке .ai id генерируются заново —
   индивидуальные смещения/исключения не находились. Теперь в op_attrs
   пишется ещё и имя операции, и при восстановлении есть запасное
   сопоставление по имени.

3) Не сохранялся `preferred_lead_side` — ручной выбор стороны захода на
   конкретном элементе. Добавлен.

Итого в сборке хранится по каждому заказу: угол, пятка, высота, ABS,
направление (гориз/верт), реверс, лимит длины, реперная X + по каждой
операции: excluded, lead_override (индивидуальные смещения/углы/длины),
preferred_lead_side.

Проверено сквозняком: направление=вертикаль, offset=-7, сторона=left
сохраняются в файл и восстанавливаются. Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## 1. Розовая подсветка угла/высоты после загрузки — снята
Значения из сборки применялись, но подсветка «не задано» не снималась
(она снималась только в XML-пути). Теперь при восстановлении настроек
заказа подсветка убирается, если значение непустое (угол ≠ «—», высота > 0).

## 2. Индивидуальные заходы при КОПИЯХ заказа
При копиях макета имена операций повторяются («Blade Knife_1» есть в каждой
копии), и восстановление по имени (v1.6.43) попадало в чужую копию —
поэтому заходы возвращались «по умолчанию». Теперь применение op_attrs
ограничено ножами/реперами региона ИМЕННО этого заказа.

Тесты 143/143.

ОСТАЁТСЯ (следующим шагом): галка вкл/выкл показа перебегов станка.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Порядок обхода после загрузки — по направлению заказа
`sort_by_grid()` вызывался с дефолтом (всегда столбцы), из-за чего после
загрузки пересчёт шёл «задом наперёд» независимо от сохранённого
направления.

Теперь порядок задаётся направлением (у тебя их два):
- Горизонтально (слева-направо) → grouping="rows";
- Вертикально (снизу-вверх) → grouping="columns";
старт всегда от левого-нижнего (direction="LB").

Пересортировка выполняется и при загрузке сборки, и при восстановлении
настроек заказа (направление применяется позже создания операций).

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Индивидуальные заходы больше не затираются авто-подбором
Ключевая причина, почему ручные настройки «возвращались по умолчанию»:
во вьювере `lead_override` использовался только как ИСТОЧНИК параметров
(угол/длина/смещение), но авто-подбор при этом оставался ВКЛЮЧЁН — и в
режиме «Авто-подбор» он двигал лид, перебивая ручную настройку. В эмиттере
такая защита была (_has_user_override), во вьювере — нет.

Теперь: если у операции есть lead_override (оператор задал заход вручную),
авто-подбор её не трогает. Ручная настройка элемента побеждает.

Вместе с v1.6.43/44 (сохранение настроек активного заказа, сопоставление
по имени с ограничением по региону копии) индивидуальные заходы теперь и
сохраняются, и восстанавливаются, и переживают пересчёт.

Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## НАСТОЯЩАЯ причина: ручные заходы стирались при пересчёте
Нашлось: в режимах «Авто-подбор» и «Все элементы» при КАЖДОМ построении
путей вызывается `_clear_lead_overrides()` — он намеренно снимает ВСЕ
ручные override'ы (и из op.attributes, и из _order_settings), чтобы после
ручных правок «Авто + Пересчитать» считал заново.

А при загрузке сборки я как раз пересчитываю пути — и этот clear стирал
только что восстановленные из файла индивидуальные смещения. Поэтому,
сколько ни чинил сохранение, на экране были умолчания.

Теперь на время восстановления сборки ставится флаг
`_restoring_saved_state`, и `_clear_lead_overrides()` при загрузке НЕ
вызывается (обычное ручное переключение режимов работает как прежде).

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Галка «Показывать перебеги станка»
В панели (блок «Генерировать файлы», под сглаживанием) появилась галка
**«Показывать перебеги станка»** — по умолчанию ВЫКЛЮЧЕНА.

Включённая рисует холостые перемещения между проходами: серый пунктир от
точки выхода предыдущего прохода ко входу следующего, в порядке обработки.
Видно, много ли станок ездит вхолостую (полезно при оценке заходов).

Реализация: вьювер собирает точки входа/выхода каждого прохода и по флагу
show_filter['rapids'] дорисовывает перебеги (Z=-5, под путями). Галка
подключена к обычному обновлению — переключение сразу перерисовывает.

Тесты 143/143.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Перебеги теперь ВИДНО + стрелки направления
В v1.6.48 перебеги рисовались, но их не было видно: Z=-5 (ПОД путями и
подложкой) и серый цвет на чёрном фоне.

Исправлено:
- Z=12 — поверх путей (у путей 5-8);
- цвет жёлто-серый (200,200,60), тонкий косметический пунктир;
- добавлены СТРЕЛКИ направления в середине каждого перебега (откуда→куда),
  размер наконечника подстраивается под длину перебега.

Тесты 143/143.

Файл: `camsys/ui/viewer_2d.py`.

---

# CAMSYS — Патч v1.6.52

## Перебеги: мгновенное вкл/выкл без пересчёта + стрелки меньше
1) Галка «Показывать перебеги станка» больше НЕ запускает пересчёт путей.
   Перебеги строятся всегда и помечаются (data(0)=='rapid'), а галка через
   отдельный сигнал просто переключает им видимость — мгновенно.
   (Если путей ещё нет, в статус-баре подсказка, что перебеги появятся
   после построения.)

2) Стрелки направления уменьшены ВДВОЕ (наконечник 0.3–1.0 мм вместо
   0.6–2.0), чтобы не загромождать картинку.

Тесты 143/143.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## 1. Перебеги привязаны к видимости ножей
Перебег теперь помнит, между какими операциями он идёт, и показывается
только если видны ОБА его конца — раньше при скрытии ножа перебеги
«висели в воздухе».

## 2. Направление — в одну строку
Радиокнопки «→ Горизонтально» и «↑↑ Вертикально» размещены горизонтально
(место в панели есть), блок стал компактнее.

## 3. «Режим применения» — НАД «Точкой входа/выхода»
Группы поменяны местами: режим определяет, к чему применяются поля захода,
логичнее выбирать его первым.

Тесты 143/143.

Файлы: `camsys/ui/viewer_2d.py`, `camsys/ui/main_window.py`.

---

# CAMSYS — Патч v1.6.52

## Перебеги всплывали при показе заказа с выключенной галкой
Обе функции показа/скрытия заказов (переключение заказа и «показать ещё
заказ») делали `setVisible(visible)` для ВСЕХ элементов кэша, включая
перебеги — поэтому при включении визуализации второго ножа перебеги
появлялись, хотя галка снята.

Теперь в обоих местах перебеги подчиняются своей галке:
видны = (заказ показан) И (галка «Показывать перебеги» включена).

Тесты 143/143.

Файл: `camsys/ui/main_window.py`.
